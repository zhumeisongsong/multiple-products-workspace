import { selfCareTopicsInterfaceAdapter } from './self-care-topics-interface-adapter';

describe('selfCareTopicsInterfaceAdapter', () => {
  it('should work', () => {
    expect(selfCareTopicsInterfaceAdapter()).toEqual(
      'self-care-topics-interface-adapter',
    );
  });
});
